#include <iostream>
#include <vector>
#include <cstdio>
#include <map>

using std::cin;
using std::cout;
using std::endl;
using std::vector;
using std::string;
using std::rand;
using std::begin;
using std::end;
using std::map;

typedef string::size_type sz;

void screen(sz ht, sz wid, char background = '#');
void screen(sz ht, sz wid = 80, char background);
void screen(sz ht = 50, sz wid, char background);

int main() {
    return EXIT_SUCCESS;
}