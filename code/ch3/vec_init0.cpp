#include <iostream>
#include <vector>
#include <string>

using std::cin;
using std::cout;
using std::endl;
using std::vector;
using std::string;

int main() {
    vector<string> svec;
    vector<int> ivec;
    vector<int> ivec2;
    vector<int> ivec3 = ivec;
    // vector<string> svec2(ivec2);

    vector<string> articles = {"a", "an", "the"};
    vector<string> v1{"a", "an", "the"};
}