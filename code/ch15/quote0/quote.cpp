#include "quote.h"
using namespace std;

double quote::net_price(std::size_t n) const {
    cout << "quote::net_price()" << endl;
    return -1;
}

std::string quote::isbn() const {
    return "isbn";
}
