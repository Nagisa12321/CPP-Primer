#include <iostream>
#include <vector>
#include <cstdio>
#include <map>

using std::cin;
using std::cout;
using std::endl;
using std::vector;
using std::string;
using std::rand;
using std::map;
using std::terminate;
using std::runtime_error;

void std::terminate() {
    printf("hello world!\n");
    exit(123);
}

int main() {    
    int i = 1 / 0;
}