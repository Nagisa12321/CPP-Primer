#include <iostream>
#include <vector>
#include <string>

using std::cin;
using std::cout;
using std::endl;
using std::vector;
using std::string;
using std::rand;

int main() {
    double dval; int ival; int *pi;

    // dval = ival = pi = 0;
    // cast an int* to an int

    pi = 0;
    dval = ival = (ptrdiff_t) pi;
}