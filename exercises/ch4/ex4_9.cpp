#include <iostream>
#include <vector>
#include <string>

using std::cin;
using std::cout;
using std::endl;
using std::vector;
using std::string;

int main() {
    const char *cp = "Hello world";

    if (cp && *cp) {
        // ... 

        // if cp is not a nullpointer
        // and the cp[0] is not '\0'
    }
}