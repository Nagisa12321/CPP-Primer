#include <iostream>
#include <vector>
#include <cstdio>
#include <map>
#include <cassert>

using std::cin;
using std::cout;
using std::endl;
using std::vector;
using std::string;
using std::rand;
using std::begin;
using std::end;
using std::map;

int main() {
    int cur;
    while (cin >> cur && cur != 100) { }
    assert(cin);
    return EXIT_SUCCESS;
}